function triggerCheckout(contributor) {
  return fetch('/api/checkout', {
    method: 'POST',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify({
      alias: contributor.alias,
      signal: contributor.signal,
      price: pricing[contributor.role] || 0
    })
  });
}
