Contributor Checkout → POST /storefront/transaction
   → GET /dispute-log/{id}
       → IF dispute → FLAG + manual review
   → PUT /audit-log/ammo
   → Dashboard → order + compliance status
   → IF failure → Recovery Path
       → Collect Logs → Validate Env → Retry
       → Incident → Rollback to last good ammo catalog
   → Legacy Broadcast → compliance proof snapshot (lot # + hash)
