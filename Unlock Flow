<button onclick="unlockContent('sig-202')">Unlock Premium</button>
function unlockContent(signalId) {
  fetch("/checkout", {
    method: "POST",
    body: JSON.stringify({ signalId })
  }).then(res => res.json())
    .then(data => window.location.href = data.checkoutUrl);
}
