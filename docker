FROM node:20-alpine AS build
WORKDIR /app
COPY . .
RUN npm i -D vite && npx vite build

FROM nginx:alpine
COPY --from=build /app/dist /usr/share/nginx/html
COPY ./config/nginx.conf /etc/nginx/nginx.conf

